# 🎬 컴포넌트/상태관리 과제

이번 과제에서는 컴포넌트 분리와 상태 관리, 스타일링 라이브러리 사용 등을 연습합니다.
`MSW`를 이용하여 Mock API 서버를 세팅해두었으므로, 실제 API와 유사한 흐름을 경험할 수 있습니다.

<hr><br>

## 📌 과제 목표

### 1. 컴포넌트 분리 연습

- 제가 미리 만들어둔 `App.tsx`에는 조회 + 추가 + 검색 기능이 모두 한 파일에 들어있습니다.
- 이를 적절히 분리하여 재사용 가능한 컴포넌트로 리팩토링하세요.
  - ex. `MovieList.tsx`, `MovieForm.tsx`, `SearchBar.tsx`, `Header.tsx` etc...
- 컴포넌트로 분리하고 해당 컴포넌트를 적절한 폴더로 분류해주세요.

### 2. 스타일링

- 현재는 기본적인 패딩, 색깔 정도만 인라인으로 들어가 있습니다.
  - 색, 테마는 제가 styles 폴더에 palette, theme으로 분리해두었습니다.
- 스타일링 라이브러리(ex. styled-components, emotion, tailwindcss 등)를 자유롭게 선택하여 원하는 대로 꾸며주세요.

### 3. 상태 관리

- 전역 상태 관리도 시도해보세요!
- 단, 모든 상태를 무작정 전역에 두지 말고, 전역으로 관리할만한 상태가 어떤 것인지를 판단해보고 그 상태들만을 전역으로 관리하는 식으로 로직을 합리적으로 설계하세요.

### 4. 커스텀 훅 설계

- 데이터 페칭, 상태 관리 등 공통 로직을 단일 책임을 가진 커스텀 훅으로 분리해주세요.
  - ex. `useMovies`, `useMovieSearch`, `useMovieForm` etc...

### 5. PR 템플릿 사용

- 해당 레포지토리에 PR 템플릿을 설정해두었습니다.
- PR 템플릿을 사용해 본인이 수행한 과제에 대해 자세히 작성해주세요.

<hr><br>

## 📂 폴더 구조

초기 제공되는 폴더 구조는 다음과 같습니다.

```typescript
├── 📂public
├── 📂src
│   ├── 📂components
│   ├── 📂hooks
│   ├── 📂mocks
│   ├── 📂styles
│   ├── 📂types
│   ├── App.tsx
│   ├── main.tsx
│   ├── vite-env.d.ts
┗── 📜etc (setting files)
```

- **mocks** → 수정 금지 (msw 세팅 완료)
- **styles** → 테마 및 색 팔레트 정의. 색 변경을 원하거나 수정이 필요할 경우 수정 가능
- **components, hooks, types** → 더미 파일만 있음. 클론 후 더미 파일을 지우고 이 폴더들에 컴포넌트를 적절히 분리해주세요.
- 필요하다면 **store, models, constants** 등 새로운 폴더를 자유롭게 추가해도 됩니다.

<hr><br>

## 🚀 주요 기능 (구현되어있음)

### 1. 영화 목록 조회

- API(`/api/movies`)에서 영화 목록을 가져와 화면에 표시합니다.
- 로딩 중임을 화면에 표시합니다.
- API 요청 실패 시 에러 메시지를 표시합니다.

### 2. 영화 검색

- 영화 제목을 기준으로 검색어와 부분 일치하는 영화만 목록에 표시합니다.
- 검색어가 없을 경우 전체 영화 목록을 보여줍니다.

### 3. 영화 추가

- 새로운 영화를 추가할 수 있는 폼을 제공합니다.
  - 필수 입력값: 제목, 감독, 연도, 장르, 평점(0~10)
- 모든 값이 입력되지 않으면 추가되지 않고, 에러 메시지를 표시합니다.
- 성공적으로 추가되면 목록이 갱신됩니다.

### 4. 테마 변경

- 다크/라이트 모드를 전환할 수 있습니다.
- 전체 화면의 배경색, 텍스트 색, 컴포넌트 색상 등이 테마에 따라 변경됩니다.

<hr><br>

## ✅ 과제 제출 방법

- 본 repository를 clone한 이후 본인 이름으로 branch를 생성 후 작업
  - ex. git checkout -b `choyeon2e`
- 과제 수행 후 본인 브랜치에 push
- develop 브랜치에 Pull Request

<hr><br>

## 💡 참고

- msw로 Mock API 서버가 이미 설정되어 있어 별도 백엔드 환경은 필요하지 않습니다.
- 추가로 에러 핸들링, 로딩 상태 표시 등을 개선해보면 좋을 것 같아요!
- UI는 기존의 것을 모두 무시해도 되니 자유롭게 꾸며보세요.
- 아래 커밋 가이드라인을 참고하여 커밋을 올려주세요! 과제 확인 시 이것도 확인하겠습니다.

### 💾 커밋 가이드라인

작은 단위로 커밋하는 습관을 들이면, 코드 관리와 리뷰가 훨씬 수월해집니다.<br>
아래와 같은 기준 단위로 커밋해주세요!

#### 1. 기능 단위 커밋

기능 추가/완료 시 커밋 <br>
ex. feat: 영화 목록 조회 기능 구현<br>

#### 2. UI/스타일 단위 커밋

스타일 적용/변경 시 커밋<br>
ex. style: 다크 모드 버튼 스타일 적용<br>

#### 3. 리팩토링 단위 커밋

컴포넌트 분리, 상태 구조 변경 등 리팩토링 시 커밋<br>
ex. refactor: MovieList 컴포넌트 분리, refactor: 테마 상태 전역으로 이동<br>

#### 4. 버그/오류 수정

오류 수정 시 커밋<br>
ex. fix: 영화 추가 시 숫자 입력 오류 수정<br>

#### 🔹 TIP

한 커밋에는 한 가지 목적만 담기<br>
커밋 메시지는 명확하고 현재 변경 내용을 설명해야 함

<hr><br>

## PR 템플릿

```
## 🧩 과제 수행 자가 체크
- [ ] 컴포넌트 분리
- [ ] 분리한 컴포넌트를 적절한 폴더로 분류하기
- [ ] 커스텀훅 분리
- [ ] 스타일링 라이브러리를 이용한 UI 변경
- [ ] 전역 상태 관리 적용 (필요한 상태만 합리적으로 전역화)
- [ ] 커밋 가이드에 맞춰서 작은 단위로 커밋하기

---

## 🛠 주요 리팩토링 포인트
### 1. 컴포넌트 구조
- `[새로 생성된 컴포넌트명]` - 담당 기능: `[컴포넌트가 담당하는 기능 상세 작성]`
> 예: `MovieList` - 영화 목록을 출력하고, 로딩/에러 처리 담당
- **분리 이유**: `[ex. 단일 책임 원칙, 재사용성, 가독성 등]`

### 2. 커스텀 훅
- **분리된 훅**: `[새로 생성된 훅 이름]`
> 예: `useMovies` - 영화 목록 조회, 추가, 검색 로직
- **분리 근거**: `[상태 로직 그룹화, 관심사 분리, 재사용성 등]`

### 3. 전역 상태 관리
- **구현 기능**: `[ex. 다크/라이트 모드 테마 변경]`
- **사용한 기술**: `[Context API, Zustand, Recoil 등 사용한 기술 명시]`
- **설계 이유**: [기술 선택 이유 및 설계 이유 작성]

---

## ✨ UI 변경사항 (스크린샷 or GIF 첨부)
> 변경된 UI를 시각적으로 보여주세요!

---

## 🧐 고민한 부분 / 다른 파트원에게 질문
> ex. MovieForm과 MovieList 간 상태 공유를 어떤 방식으로 하면 좋을지 궁금합니다.
> ex. UI/UX 개선 아이디어가 있으면 공유 부탁드립니다.
-
-


```
